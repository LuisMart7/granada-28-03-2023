import "./style.css";

/**
 * Este componente es de ejemplo y solo sirve para marcar sitios en el layout.
 */
export const Table = ({ width, height }) => {
    return (
        <div className="ranking__table" style={{ width, height }}>
            Esta es la tabla
        </div>
    )
}